# Notices

MouthClick is released under the MIT License.

The file `models/face_landmarker.task` is a third-party MediaPipe model asset used locally for face landmark detection. It is included so the prototype can run without downloading the model at startup.

OpenCV, MediaPipe and PyAutoGUI are third-party dependencies and keep their own licenses.
